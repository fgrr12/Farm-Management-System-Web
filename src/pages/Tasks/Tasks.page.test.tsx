import { beforeEach, describe, expect, it, vi } from 'vitest'

describe('Tasks Page', () => {
	beforeEach(() => {
		vi.clearAllMocks()
	})

	it('should be tested when component is properly mocked', () => {
		expect(true).toBe(true)
	})
})
